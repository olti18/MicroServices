
server:
    port: 8089

spring:
    datasource:
        url: jdbc:h2:mem:testdb
        username: sa
        password: ''
    h2:
      console.enabled: true
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      hibernate:
        ddl-auto: update
      show-sql: true
